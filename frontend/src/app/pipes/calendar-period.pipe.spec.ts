import { CalendarPeriodPipe } from './calendar-period.pipe';

describe('CalendarPeriodPipe', () => {
  it('create an instance', () => {
    const pipe = new CalendarPeriodPipe();
    expect(pipe).toBeTruthy();
  });
});
